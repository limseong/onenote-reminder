package com.limseong.onenotereminder.util;

import android.view.View;

import com.limseong.onenotereminder.R;

import androidx.fragment.app.Fragment;
import androidx.fragment.app.FragmentActivity;

public class FragmentUtils {
/*
    public static void showProgressBar(Fragment fragment, int progressBar, int fragmentContainer) {
        final FragmentActivity activity = fragment.getActivity();
        activity.runOnUiThread(new Runnable() {
            @Override
            public void run() {
                activity.findViewById(R.id.progressbar)
                        .setVisibility(View.VISIBLE);
                activity.findViewById(R.id.fragment_container)
                        .setVisibility(View.GONE);
            }
        });
    }

    private void hideProgressBar(Fragment fragment, int progressBar, int fragmentContainer) {
        final FragmentActivity activity = fragment.getActivity();
        activity.runOnUiThread(new Runnable() {
            @Override
            public void run() {
                activity.findViewById(progressBar)
                        .setVisibility(View.GONE);
                activity.findViewById(R.id.fragment_container)
                        .setVisibility(View.VISIBLE);
            }
        });
    }*/
}
